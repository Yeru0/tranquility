import { GoogleDirective } from './google.directive';

describe('GoogleDirective', () => {
  it('should create an instance', () => {
    const directive = new GoogleDirective();
    expect(directive).toBeTruthy();
  });
});
